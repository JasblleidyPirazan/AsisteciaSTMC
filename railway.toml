[build]
  builder = "nixpacks"
  buildCommand = "cd client && npm install --include=dev && npm run build && cd ../server && npm install --include=dev && npx prisma generate --schema src/prisma/schema.prisma"

[deploy]
  startCommand = "cd server && sh start.sh"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3

[env]
  NODE_ENV = "production"
  PORT = "3000"
